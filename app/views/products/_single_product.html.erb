<h3><%= link_to 'Add a New Product', new_product_path%></h3>
<% @products.each do |product| %>
  <h3>Name: <%= product.name %></h2>
  <h3>Price: <%= product.price %></h3>
  <h3>Discription: <%= product.discription %></h3>
  <h3><%=image_tag product.image %></h3>
  <%= link_to 'Edit', edit_product_path(product) %> <%= link_to 'Delete', product_path(product), method: :delete, data: {confirm: "Are you sure?" } %> </p>
  <br>
<% end %>
